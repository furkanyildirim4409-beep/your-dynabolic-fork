const SettingsPanel = ({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) => { if (!isOpen) return null; return <div className="fixed inset-0 z-50 bg-black/80" onClick={onClose}><div className="absolute right-0 top-0 bottom-0 w-full max-w-sm bg-background p-6" onClick={e => e.stopPropagation()}><h2 className="font-display text-lg text-foreground mb-4">AYARLAR</h2><p className="text-muted-foreground text-sm">Ayarlar paneli (Yakında)</p><button onClick={onClose} className="mt-4 bg-primary text-primary-foreground px-6 py-2 rounded-xl font-display">KAPAT</button></div></div>; };
export default SettingsPanel;
