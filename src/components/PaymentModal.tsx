const PaymentModal = ({ isOpen, onClose, payment, onPaymentSuccess }: { isOpen: boolean; onClose: () => void; payment: any; onPaymentSuccess: () => void }) => { if (!isOpen || !payment) return null; return <div className="fixed inset-0 z-[120] bg-black/80 flex items-center justify-center p-4" onClick={onClose}><div className="bg-background border border-white/10 rounded-2xl p-6 max-w-sm w-full" onClick={e => e.stopPropagation()}><h3 className="font-display text-lg text-foreground mb-2">ÖDEME</h3><p className="text-muted-foreground text-sm mb-4">{payment.title} - {payment.amount}₺</p><button onClick={() => { onPaymentSuccess(); onClose(); }} className="w-full bg-primary text-primary-foreground py-3 rounded-xl font-display">ÖDE</button><button onClick={onClose} className="w-full mt-2 text-muted-foreground py-2 text-sm">İptal</button></div></div>; };
export type PaymentDetails = { amount: number; title: string; description?: string; type: string; referenceId: string; };
export default PaymentModal;
