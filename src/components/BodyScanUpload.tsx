const BodyScanUpload = ({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) => { if (!isOpen) return null; return <div className="fixed inset-0 z-50 bg-black/80 flex items-center justify-center" onClick={onClose}><div className="bg-background p-6 rounded-2xl max-w-sm" onClick={e => e.stopPropagation()}><h3 className="font-display text-foreground mb-4">Vücut Taraması</h3><p className="text-muted-foreground text-sm">Fotoğraf yükleme (Demo)</p><button onClick={onClose} className="mt-4 w-full bg-primary text-primary-foreground py-3 rounded-xl font-display">KAPAT</button></div></div>; };
export default BodyScanUpload;
