const PaymentReceiptModal = ({ isOpen, onClose, invoice }: { isOpen: boolean; onClose: () => void; invoice: any }) => { if (!isOpen || !invoice) return null; return <div className="fixed inset-0 z-[120] bg-black/80 flex items-center justify-center p-4" onClick={onClose}><div className="bg-background border border-white/10 rounded-2xl p-6 max-w-sm w-full" onClick={e => e.stopPropagation()}><h3 className="font-display text-lg text-foreground mb-2">MAKBUZ</h3><p className="text-foreground">{invoice.serviceType}</p><p className="text-primary font-display text-xl mt-2">{invoice.amount}₺</p><button onClick={onClose} className="w-full mt-4 bg-primary text-primary-foreground py-3 rounded-xl font-display">KAPAT</button></div></div>; };
export default PaymentReceiptModal;
